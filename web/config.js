// Site configuration.
// jevEndpoint: URL of the deployed /api/decide function (see "Jev features" in the README), for example
// 'https://your-project.vercel.app/api/decide'. Leave it empty and every feature falls back to plain
// matching in the browser; nothing is sent anywhere.
window.QTA_CONFIG = { jevEndpoint: '' };
